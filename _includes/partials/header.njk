<header class="brutal-header text-center py-5">
    <div class="container position-relative">
      <div class="header-grid-lines"></div>
      <div class="header-content">
        <h1 class="mega-title glitch-title" data-text="{{metadata.title}}">{{metadata.title}}</h1>
        <p class="tagline typewriter">{{metadata.description}}</p>
      </div>
      <div class="brutal-badge rotate-element">{{metadata.slogan}}</div>
    </div>
  </header>